source "https://rubygems.org"

# Alinha o ambiente local com o ambiente de execução do GitHub Pages
gem "github-pages", group: :jekyll_plugins

# Servidor HTTP padrão necessário para Ruby 3+ no comando `jekyll serve`
gem "webrick"
